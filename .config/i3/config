# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:FantasqueSansMono Nerd Font 8

# Lock Screen
exec --no-startup-id xss-lock -- $HOME/.config/i3/lock.sh 
bindsym $mod+shift+l exec --no-startup-id loginctl lock-session ${XDG_SESSION_ID-}


# Laptop DPI bug- Envycontrol i think
exec --no-startup-id xrandr --dpi 96x96

# Use pactl to adjust volume in PulseAudio.
#set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% 
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle 
bindsym $mod+Shift+F9 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ 100%
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Media player controls
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause -p spotify
#bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec --no-startup-id playerctl next -p spotify
bindsym XF86AudioPrev exec --no-startup-id playerctl previous -p spotify
#bindsym XF86AudioStop exec playerctl stop

## Clipboard Screenshots
bindsym Print exec --no-startup-id path="$HOME/Pictures/Screenshots/Temporary/$(date).png" && maim -s "$path" && xclip -selection clipboard -t image/png "$path" 

## Save Screenshots
bindsym Shift+Print exec --no-startup-id sleep 0.5 && path="$HOME/Pictures/Screenshots/$(date).png" && maim "$path" && xclip -selection clipboard -t image/png "$path" 
bindsym $mod+Print exec --no-startup-id path="$HOME/Pictures/Screenshots/$(date).png" && maim -s "$path" && xclip -selection clipboard -t image/png "$path"

# colors
# Theme colors
# class                   border  backgr. text    indic.  child_border
client.focused            #FF80BF #544158 #F8F8F2 #FF80BF #FF80BF
client.focused_inactive   #313244 #2A212C #F8F8F2 #313244 #313244
client.unfocused          #313244 #2A212C #F8F8F2 #313244 #313244
client.urgent             #FF9580 #2A212C #F8F8F2 #FF9580 #FF9580
client.placeholder        #544158 #2A212C #F8F8F2 #544158 #282A36
client.background         #F8F8F2

# Configure border style <normal|1pixel|pixel xx|none|pixel>
default_border pixel 3
default_floating_border pixel 3

# Hide borders
hide_edge_borders none

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty

# kill focused window
bindsym $mod+Shift+q kill

# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# start rofi
bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym $mod+Shift+d exec --no-startup-id rofi -show run
bindsym $mod+Tab exec --no-startup-id rofi -show window
bindsym $mod+Shift+e exec --no-startup-id rofi -show powermenu -modi powermenu:~/.config/rofi/rofi-power-menu

# gaps
gaps inner 5
gaps outer 3

# change focus
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
#bindsym $mod+Shift+j move left
#bindsym $mod+Shift+k move down
#bindsym $mod+Shift+l move up
#bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+shift+f fullscreen toggle

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1 "
set $ws2 "2 "
set $ws3 "3 ﱮ"
set $ws4 "4 "
set $ws5 "5 "
set $ws6 "6 "
set $ws7 "7 "
set $ws8 "8 阮"
set $ws9 "9 "
set $ws10 "10 ﳨ"
set $ws0 "0 " 

# assign to workspace
#assign [class="Alacritty"] $ws1
#assign [class="Thunar"] $ws3
#assign [class="firefox"] $ws4
assign [class="discord"] $ws5
#assign [class="Mousepad"] $ws6
#assign [class="Spotify"] $ws8
#assign [class="Minecraft* 1.19"] $ws0
assign [class="MultiMC"] $ws0

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindsym $mod+grave workspace number $ws0

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
bindsym $mod+Shift+grave move container to workspace number $ws0
# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize grow height 1 px or 1 ppt
        bindsym Up resize shrink height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

	# Increase size by 5
        bindsym Shift+Left resize shrink width 5 px or 5 ppt
        bindsym Shift+Down resize grow height 5 px or 5 ppt
        bindsym Shift+Up resize shrink height 5 px or 5 ppt
        bindsym Shift+Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Starts the bar
exec_always --no-startup-id $HOME/.config/i3/polybar/launch.sh

#bar {
#        position top
#	status_command i3status
#}


# Picom Settings
bindsym $mod+Shift+F11 exec --no-startup-id $HOME/.config/i3/picom/picom.sh
bindsym $mod+shift+F12 exec --no-startup-id killall 'picom'

# Actually quit discord
bindsym $Mod+shift+F10 exec --no-startup-id killall 'Discord'

# App Keybinds
bindsym $mod+e exec --no-startup-id thunar
bindsym $mod+f exec --no-startup-id firefox

# Brightnessctl
#bindsym $mod+shift+F2 exec brightnessctl s 5%-
#bindsym $mod+shift+F1 exec brightnessctl s 5%+

# Autostart
exec_always --no-startup-id $HOME/.config/i3/picom/picom.sh
exec_always --no-startup-id systemctl --user restart dunst
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
#xborder for rounded corners on i3
exec_always --no-startup-id killall -q xborders && $HOME/.config/i3/xborder/xborders --border-rgba F5C2E7AA --border-width 4
#--config $HOME/.config/i3/xborder/example_config.json
# Feh Wallpaper
exec_always --no-startup-id ~/.fehbg

# Window config
for_window [class="Image Lounge"] floating enable
